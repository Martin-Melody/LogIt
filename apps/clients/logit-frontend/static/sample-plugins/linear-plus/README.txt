Linear Plus sample plugin

This sample plugin demonstrates the v1 plugin bundle contract.
It is safe to import locally from the sample manifest URL:

  /sample-plugins/linear-plus/manifest.json

The bundle is a plain JavaScript module that exports:

  - pluginBundle
  - algorithm

