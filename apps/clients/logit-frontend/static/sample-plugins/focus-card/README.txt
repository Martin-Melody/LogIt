Focus Card sample widget

This sample plugin demonstrates the widget-side v1 bundle contract.
Import it from:

  /sample-plugins/focus-card/manifest.json

The bundle exports:

  - pluginBundle
  - widget

The widget export exposes a renderHtml() function, which the app hosts inside a
generic plugin widget wrapper.

